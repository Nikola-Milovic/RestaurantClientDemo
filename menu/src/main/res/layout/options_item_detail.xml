<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="price"
            type="com.nikolam.core.model.Price" />
    </data>


    <LinearLayout
        android:orientation="horizontal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/materialCardView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/option_TextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="10sp"
            android:text="@{price.option}"
            android:textSize="30dp"
            tools:text="@tools:sample/cities" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/price_TextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="10sp"
            android:text="@{String.valueOf(price.price)}"
            android:textSize="30dp"
            tools:text="@tools:sample/cities" />

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/add_actionButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="10sp"
            android:src="@drawable/ic_baseline_add_24"
            android:tint="@color/cardview_dark_background"
            app:fabSize="mini" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/amount_InputText"
            android:textStyle="bold"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="10sp"
            android:text="@{String.valueOf(price.amount)}"
            android:textSize="30dp"
            tools:text="@tools:sample/cities" />



        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/subtract_actionButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_baseline_minimize_24"
            android:tint="@color/cardview_dark_background"
            app:fabSize="mini" />


    </LinearLayout>

</layout>
